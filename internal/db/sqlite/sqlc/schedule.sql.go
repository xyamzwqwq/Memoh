// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: schedule.sql

package sqlc

import (
	"context"
	"database/sql"
)

const createSchedule = `-- name: CreateSchedule :one
INSERT INTO schedule (id, name, description, pattern, max_calls, enabled, command, bot_id)
VALUES (
  lower(hex(randomblob(4))) || '-' ||
  lower(hex(randomblob(2))) || '-' ||
  '4' || substr(lower(hex(randomblob(2))), 2) || '-' ||
  substr('89ab', abs(random()) % 4 + 1, 1) || substr(lower(hex(randomblob(2))), 2) || '-' ||
  lower(hex(randomblob(6))),
  ?1,
  ?2,
  ?3,
  ?4,
  ?5,
  ?6,
  ?7
)
RETURNING id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
`

type CreateScheduleParams struct {
	Name        string        `json:"name"`
	Description string        `json:"description"`
	Pattern     string        `json:"pattern"`
	MaxCalls    sql.NullInt64 `json:"max_calls"`
	Enabled     int64         `json:"enabled"`
	Command     string        `json:"command"`
	BotID       string        `json:"bot_id"`
}

func (q *Queries) CreateSchedule(ctx context.Context, arg CreateScheduleParams) (Schedule, error) {
	row := q.db.QueryRowContext(ctx, createSchedule,
		arg.Name,
		arg.Description,
		arg.Pattern,
		arg.MaxCalls,
		arg.Enabled,
		arg.Command,
		arg.BotID,
	)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Pattern,
		&i.MaxCalls,
		&i.CurrentCalls,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Enabled,
		&i.Command,
		&i.BotID,
	)
	return i, err
}

const deleteSchedule = `-- name: DeleteSchedule :exec
DELETE FROM schedule WHERE id = ?1
`

func (q *Queries) DeleteSchedule(ctx context.Context, id string) error {
	_, err := q.db.ExecContext(ctx, deleteSchedule, id)
	return err
}

const getScheduleByID = `-- name: GetScheduleByID :one
SELECT id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
FROM schedule
WHERE id = ?1
`

func (q *Queries) GetScheduleByID(ctx context.Context, id string) (Schedule, error) {
	row := q.db.QueryRowContext(ctx, getScheduleByID, id)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Pattern,
		&i.MaxCalls,
		&i.CurrentCalls,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Enabled,
		&i.Command,
		&i.BotID,
	)
	return i, err
}

const incrementScheduleCalls = `-- name: IncrementScheduleCalls :one
UPDATE schedule
SET current_calls = current_calls + 1,
    enabled = CASE
      WHEN max_calls IS NOT NULL AND current_calls + 1 >= max_calls THEN false
      ELSE enabled
    END,
    updated_at = CURRENT_TIMESTAMP
WHERE id = ?1
RETURNING id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
`

func (q *Queries) IncrementScheduleCalls(ctx context.Context, id string) (Schedule, error) {
	row := q.db.QueryRowContext(ctx, incrementScheduleCalls, id)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Pattern,
		&i.MaxCalls,
		&i.CurrentCalls,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Enabled,
		&i.Command,
		&i.BotID,
	)
	return i, err
}

const listEnabledSchedules = `-- name: ListEnabledSchedules :many
SELECT id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
FROM schedule
WHERE enabled = true
ORDER BY created_at DESC
`

func (q *Queries) ListEnabledSchedules(ctx context.Context) ([]Schedule, error) {
	rows, err := q.db.QueryContext(ctx, listEnabledSchedules)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Schedule
	for rows.Next() {
		var i Schedule
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Pattern,
			&i.MaxCalls,
			&i.CurrentCalls,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Enabled,
			&i.Command,
			&i.BotID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listSchedulesByBot = `-- name: ListSchedulesByBot :many
SELECT id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
FROM schedule
WHERE bot_id = ?1
ORDER BY created_at DESC
`

func (q *Queries) ListSchedulesByBot(ctx context.Context, botID string) ([]Schedule, error) {
	rows, err := q.db.QueryContext(ctx, listSchedulesByBot, botID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Schedule
	for rows.Next() {
		var i Schedule
		if err := rows.Scan(
			&i.ID,
			&i.Name,
			&i.Description,
			&i.Pattern,
			&i.MaxCalls,
			&i.CurrentCalls,
			&i.CreatedAt,
			&i.UpdatedAt,
			&i.Enabled,
			&i.Command,
			&i.BotID,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Close(); err != nil {
		return nil, err
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateSchedule = `-- name: UpdateSchedule :one
UPDATE schedule
SET name = ?1,
    description = ?2,
    pattern = ?3,
    max_calls = ?4,
    enabled = ?5,
    command = ?6,
    updated_at = CURRENT_TIMESTAMP
WHERE id = ?7
RETURNING id, name, description, pattern, max_calls, current_calls, created_at, updated_at, enabled, command, bot_id
`

type UpdateScheduleParams struct {
	Name        string        `json:"name"`
	Description string        `json:"description"`
	Pattern     string        `json:"pattern"`
	MaxCalls    sql.NullInt64 `json:"max_calls"`
	Enabled     int64         `json:"enabled"`
	Command     string        `json:"command"`
	ID          string        `json:"id"`
}

func (q *Queries) UpdateSchedule(ctx context.Context, arg UpdateScheduleParams) (Schedule, error) {
	row := q.db.QueryRowContext(ctx, updateSchedule,
		arg.Name,
		arg.Description,
		arg.Pattern,
		arg.MaxCalls,
		arg.Enabled,
		arg.Command,
		arg.ID,
	)
	var i Schedule
	err := row.Scan(
		&i.ID,
		&i.Name,
		&i.Description,
		&i.Pattern,
		&i.MaxCalls,
		&i.CurrentCalls,
		&i.CreatedAt,
		&i.UpdatedAt,
		&i.Enabled,
		&i.Command,
		&i.BotID,
	)
	return i, err
}
